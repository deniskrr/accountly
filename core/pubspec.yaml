name: core
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.15.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  auto_route: ^3.2.2
  get_it: ^7.2.0
  feature_modules:
    path: '../feature_modules'

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  auto_route_generator: ^3.2.1
  build_runner: ^2.1.7

flutter:
  uses-material-design: true